#include <SFML/Graphics.hpp>


int main(int argc, char** argv)
{
  sf::RenderWindow renderWindow(sf::VideoMode(1024, 768), "Cornell Box", sf::Style::Default, sf::ContextSettings(32, 32, 0, 4, 5));

  return 0;
}
